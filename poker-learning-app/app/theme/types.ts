export interface ThemeColors {
  background: string;
  card: string;
  input: string;
  primary: string;
  primaryText: string;
  text: string;
  secondaryText: string;
  border: string;
  buttonInactive: string;
  danger: string;
}
